//const date = new Date().toLocaleDateString();
const CaughtPokemon = (props) => {
    return (
      <div>
        <p>Caught 0 Pokemon on {props.dateInput}</p>
      </div>
    );
  };
  export default CaughtPokemon;